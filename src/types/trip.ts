export type TTrip = {
    id: string
    userId: string
    destination: string
    startDate: string
    endDate: string
    budget: number
    activities: string[]
    createdAt: string
    updatedAt: string
    photos: string[]
    description: string
    type: string
}